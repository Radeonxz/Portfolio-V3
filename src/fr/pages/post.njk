---
title: Xuan.Z
layout: layouts/base
permalink: /{{ locale }}/post/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}index.html
pagination:
  data: collections.posts_fr
  size: 3
  alias: posts
  reverse: true
---

<div class="section container">
  <h1 class="section-title">{{ "hello" | i18n }}</h1>
  <h2 class="section-title">{{ "hello" | i18n }}</h2>
  <h3 class="section-title">{{ "hello" | i18n }}</h3>
  <h4 class="section-title">{{ "hello" | i18n }}</h4>
  <h5 class="section-title">{{ "hello" | i18n }}</h5>
  <h6 class="section-title">{{ "hello" | i18n }}</h6>
  <ul role="list" class="articles_list flow">
    {%- for post in posts -%} {% include 'layouts/article-snippet.njk' %} {%-
    endfor -%}
  </ul>
</div>

{% include 'layouts/pagination.njk' %}
